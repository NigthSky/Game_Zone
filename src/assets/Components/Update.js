import React from "react";

function Update(props) {
    console.log('update');
    return(
        <h1>x</h1>
    );
    // useEffect(() => {
    //     localStorage.setItem('Cartitem', JSON.stringify(addItem));
    // }, [addItem]);
    
}

export default Update;